/*document.getElementById("rangeEstimatorForm").addEventListener("submit", function(event){
    event.preventDefault();
    let batteryCapacity = document.getElementById("batteryCapacity").value;
    let consumptionRate = document.getElementById("consumptionRate").value;

    let estimatedRange = batteryCapacity * 1000 / consumptionRate;

    document.getElementById("outputRange").textContent = estimatedRange.toFixed(2) + " miles";
});*/
